# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1


set $resize_value 5

set $w1 1
set $w2 2
set $w3 3
set $w4 4
set $w5 5
set $w6 6
set $w7 7
set $w8 8
set $w9 9
set $w10 10
set $www  www
set $work work

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
#font pango:DejaVu Sans Mono 8

#font pango:Cantarell 8
#font pango:Nanum Gothic 8
#font pango:PT Sans 8
font -xos4-terminus-medium-r-normal--12-120-72-72-c-60-iso10646-1


# colors
set $background  background         #000000
#set $statusline  statusline         #7799BB
#set $statusline  statusline         #AAAAAA
set $statusline  statusline         #FFFFFF
set $barcolor1   focused_workspace  #ffffff #285577
set $barcolor2   active_workspace   #ffffff #333333
set $barcolor3   inactive_workspace #888888 #222222
set $barcolor4   urgent_workspace   #ffffff #900000


## class                 border  backgr. text
client.focused           #2B6473 #2B6473 #ffffff
client.focused_inactive  #004050 #004050 #ffffff
client.unfocused         #004050 #004050 #cccccc
client.urgent            #900000 #900000 #ffffff
client.background        #342826


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return       exec smowterm
bindsym $mod+Shift+Return exec smowterm gui
#bindsym $mod+Return       exec urxvtc -e sh -c "/usr/bin/screen -OxR main"
#bindsym $mod+Shift+Return exec urxvtc -name URxvtRemote -e sh -c "~/bin/screen_remote z8"
#bindsym $mod+Return       exec dbus-launch --exit-with-session urxvtc -e sh -c "/usr/bin/screen -OxR main"

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -nb '#000000' -nf '#ffffff' -sb '#000000' -sf '#1793d0' -p  'Run: ' -fn '-xos4-terminus-medium-r-normal--12-120-72-72-c-60-iso8859-1'
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# move to output
bindsym $mod+Control+h move workspace to output left
bindsym $mod+Control+j move workspace to output down
bindsym $mod+Control+k move workspace to output up
bindsym $mod+Control+l move workspace to output right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# focus last urgent
bindsym $mod+Escape [urgent="last"] focus

# switch to workspace
bindsym $mod+1   workspace $w1
bindsym $mod+2   workspace $w2
bindsym $mod+3   workspace $w3
bindsym $mod+4   workspace $w4
bindsym $mod+5   workspace $w5
bindsym $mod+6   workspace $w6
bindsym $mod+7   workspace $w7
bindsym $mod+8   workspace $w8
bindsym $mod+9   workspace $w9
bindsym $mod+0   workspace $w10
bindsym $mod+F11 workspace $www
bindsym $mod+F12 workspace $work

# switch between the current and the previously focused one
bindsym $mod+b workspace back_and_forth

# move focused container to workspace
bindsym $mod+Shift+1   move container to workspace $w1
bindsym $mod+Shift+2   move container to workspace $w2
bindsym $mod+Shift+3   move container to workspace $w3
bindsym $mod+Shift+4   move container to workspace $w4
bindsym $mod+Shift+5   move container to workspace $w5
bindsym $mod+Shift+6   move container to workspace $w6
bindsym $mod+Shift+7   move container to workspace $w7
bindsym $mod+Shift+8   move container to workspace $w8
bindsym $mod+Shift+9   move container to workspace $w9
bindsym $mod+Shift+0   move container to workspace $w10
bindsym $mod+Shift+F11 move container to workspace $www
bindsym $mod+Shift+F12 move container to workspace $work

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width $resize_value px or $resize_value ppt
        bindsym j resize grow height $resize_value px or $resize_value ppt
        bindsym k resize shrink height $resize_value px or $resize_value ppt
        bindsym l resize grow width $resize_value px or $resize_value ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width $resize_value px or $resize_value ppt
        bindsym Down resize grow height $resize_value px or $resize_value ppt
        bindsym Up resize shrink height $resize_value px or $resize_value ppt
        bindsym Right resize grow width $resize_value px or $resize_value ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"


bindsym $mod+t border normal
bindsym $mod+y border 1pixel
bindsym $mod+u border none

# Workspace settings
#workspace $w1 output LVDS1
#workspace $w2 output LVDS1
#workspace $w3 output LVDS1
#workspace $w4 output LVDS1
#workspace $w5 output LVDS1

# functions control
# key definition at /usr/share/X11/XKeysymDB

bindsym XF86ScreenSaver            exec xautolock -locknow
bindsym XF86Sleep                  exec computer suspend
bindsym XF86Hibernate              exec computer hibernate
bindsym XF86TouchpadToggle         exec toggle_touchpad

# Section to control the Master output
bindSym XF86AudioRaiseVolume       exec --no-startup-id "amixer -qM set Master 5%+"
bindSym XF86AudioLowerVolume       exec --no-startup-id "amixer -qM set Master 5%-"
bindSym XF86AudioMute              exec --no-startup-id "amixer -q  set Master toggle"

## #Section to control the Capture (mic)
#bindSym Shift+XF86AudioRaiseVolume exec --no-startup-id "amixer -qM set Capture 5%+"
#bindSym Shift+XF86AudioLowerVolume exec --no-startup-id "amixer -qM set Capture 5%-"
#bindSym Shift+XF86AudioMute        exec --no-startup-id "amixer -q  set Capture toggle"


bindsym XF86MonBrightnessUp        exec xbacklight -inc 20
bindsym XF86MonBrightnessDown      exec xbacklight -dec 20
bindsym $mod+XF86MonBrightnessUp   exec xbacklight -set 100
bindsym $mod+XF86MonBrightnessDown exec xbacklight -set 0

#bindsym XF86AudioPlay              exec zenity --info --text "Define Play"
#bindsym XF86AudioStop              exec zenity --info --text "Define Stop"
#bindsym XF86AudioPrev              exec zenity --info --text "Define Prev"
#bindsym XF86AudioNext              exec zenity --info --text "Define Next"
#bindsym XF86AudioRecord            exec zenity --info --text "Define Audio Record"

# Other binds
bindsym Print                      exec gnome-screenshot
bindsym $alt+Print                 exec gnome-screenshot --window

bindsym $mod+period                exec ~/bin/automatic gui
bindsym $mod+comma                 exec killall automatic

# Notify history
bindsym $mod+n                     exec notify

# xpad
bindsym $mod+p                     exec xpad -t
bindsym $mod+Shift+p               exec xpad -n




# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	output LVDS1
	position top
	status_command i3status --config ~/.i3/status-primary
	#status_command py3status -c ~/.i3/status-primary
	#status_command i3status --config ~/.i3/status-primary.conf | while read C; do echo "$(complement.sh austin zurich pomodoro) $C"; done
	#status_command conky -t 'CPU: ${cpu cpu1}% ${cpu cpu2}% ${cpu cpu3}% ${cpu cpu4}% ${cpu cpu5}% ${cpu cpu6}% ${cpu cpu7}% ${cpu cpu8}% - ${cpu cpu0}% | MEM: $mem/$memmax - $memperc% | SWAP: $swap/$swapmax $swapperc% | ${time %a %b %d %T %Z %Y}'
	#status_command conky | dzen2 -h ’17’ -w ’1500’ -x ’2340’ -ta ’rm’ -fn ’-xos4-terminus-medium-r-normal--12-120-72-72-c-60-iso8859-1’

	tray_output LVDS1

	colors {
		$background
		$statusline
		$barcolor1
		$barcolor2
		$barcolor3
		$barcolor4
	}
}

bar {
	output HDMI2
	position top
	#status_command i3status --config ~/.i3/status-primary
#	status_command i3status --config ~/.i3/status-secundary
#	status_command conky -w i3 -c ~/.i3/conkyrc
	tray_output none

	colors {
		$background
		$statusline
		$barcolor1
		$barcolor2
		$barcolor3
		$barcolor4
	}
}

bar {
	output VGA1
	position top
	#status_command i3status --config ~/.i3/status-primary
#	status_command i3status --config ~/.i3/status-secundary
#	status_command conky -w i3 -c ~/.i3/conkyrc
	tray_output none

	colors {
		$background
		$statusline
		$barcolor1
		$barcolor2
		$barcolor3
		$barcolor4
	}
}


# screen
bindsym $mod+F1 [class="URxvt" title="1\*"] focus
bindsym $mod+F2 [class="URxvt" title="2\*"] focus
bindsym $mod+F3 [class="URxvt" title="3\*"] focus
bindsym $mod+F4 [class="URxvt" title="4\*"] focus
bindsym $mod+F5 [class="URxvt" title="5\*"] focus
bindsym $mod+F6 [class="URxvt" title="6\*"] focus
bindsym $mod+F7 [class="URxvt" title="7\*"] focus
bindsym $mod+F8 [class="URxvt" title="8\*"] focus
bindsym $mod+F9 [class="URxvt" title="9\*"] focus
bindsym $mod+F10 [class="URxvt" title="0\*"] focus


# www
#bindsym $mod+F2 [class="Google-chrome"] focus
assign [class="Opera"] www
assign [class="Firefox"] www
assign [class="Google-chrome"] www
assign [class="Chromium-browser"] www
assign [class="Browser"] www
#for_window [class="Google-chrome" instance="crx_nckgahadagoaajjgafhacjanaoiihapd"] floating disable; layout stacking; move right; resize shrink right 31px or 31 ppt
for_window [class="Google-chrome"] border none
for_window [class="Chromium-browser"] border none
for_window [class="Firefox"] border none

# work
workspace "work" output LVDS1
assign [title="NotesLogo"] work
assign [title="(IBM|Lotus) Notes*"] work
assign [class="Notes"] work
for_window [title="NotesLogo"] floating enable
for_window [title="NotesLogo"] border none
#for_window [class="(IBM|Lotus) Notes"] floating disable
for_window [class="(IBM|Lotus) Notes" title="* is In a Meeting "] floating enable
for_window [class="(IBM|Lotus) Notes" title="Add Sametime Contact*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Add Sametime Group*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Alarm"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Alert Me When "] floating enable
for_window [class="(IBM|Lotus) Notes" title="Call*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Change Group*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Contact Added*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Edit Nickname*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Group Added*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Preferences"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Remove Groups/Contacts*"] floating enable
for_window [class="(IBM|Lotus) Notes" title="Sametime "] floating enable

# dia
assign [class="Dia"] dia
for_window [window_role="toolbox_window"] floating disable
for_window [window_role="toolbox_window"] layout toggle split
for_window [window_role="toolbox_window"] move left
for_window [window_role="toolbox_window"] resize shrink left 325px
#for_window [window_role="gimp-dock"] floating disable; layout stacking; move right; resize shrink right 31px or 31 ppt


# gimp
assign [class="Gimp"] gimp
#for_window [title="GIMP Startup"] move workspace gimp
#for_window [window_role="gimp-toolbox"] floating disable; layout stacking; move left; resize shrink right 31px or 31 ppt
#for_window [window_role="gimp-dock"] floating disable; layout stacking; move right; resize shrink right 31px or 31 ppt


# minecraft
assign [title="Magic Launcher"] mc
assign [title="Minecraft*"] mc
assign [title="MCEdit *"] mcedit
for_window [title="Minecraft*"] border none
for_window [class="magic-launcher-MagicLauncher"] floating disable; layout tabbed


# im
bindsym $mod+m workspace im
bindsym $mod+Shift+M move workspace im
workspace "im" output LVDS1
for_window [class="Pidgin"] move workspace im
for_window [class="Sametime"] move workspace im
assign [class="Google-chrome" instance="crx_nckgahadagoaajjgafhacjanaoiihapd"] im
#for_window [class="Pidgin"] floating enable


# others
for_window [class="Blueman-manager"] floating enable
for_window [class="Conky"] floating disable; move right
for_window [class="Devede"] floating enable
for_window [class="Eog"] floating enable
for_window [class="Gtk-recordMyDesktop"] floating enable
for_window [class="Ibmsaml"] floating enable
for_window [class="Istanbul"] floating enable
for_window [class="Key-mon"] border none
for_window [class="Key-mon"] floating enable
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Setroubleshoot"] floating enable
for_window [class="Shotwell"] floating enable
for_window [class="Skype"] floating enable move right 520px, move down 15px
for_window [class="X3270"] floating enable
for_window [class="XFontSel"] floating enable
for_window [class="grub-emu"] floating enable
for_window [class="xpad"] border none
for_window [class="xpad"] floating enable
for_window [title="Event Tester"] floating enable
for_window [title="VNC Viewer: Information"] floating enable
for_window [title="VNC authentication [VncAuth]"] floating enable
for_window [title="xfce4-notifyd"] border none
for_window [title="xfce4-notifyd"] floating enable
for_window [class="Gnucash"] floating enable
for_window [title="* - GnuCash"] floating disable
for_window [class="Agnclient"] floating enable
for_window [class="Arandr"] floating enable
for_window [class="qemu-system-*"] floating enable

# Start some util apps
#exec --no-startup-id /usr/libexec/gnome-settings-daemon
exec --no-startup-id /usr/local/bin/urxvtd -q -f -o
exec --no-startup-id /usr/bin/screen -AOxRR main
exec --no-startup-id /usr/bin/xfce4-power-manager
exec --no-startup-id /usr/bin/feh --bg-scale ~/Backgrounds/circuit_board-Abstract_design_wallpaper_2560x1600.jpg
exec --no-startup-id /usr/bin/xautolock
exec --no-startup-id /usr/bin/parcellite
exec --no-startup-id /home/pfsmorigo/bin/dropbox_monitor
exec --no-startup-id /usr/bin/blueman-applet
exec --no-startup-id /usr/bin/nm-applet --sm-disable
exec --no-startup-id /usr/bin/xpad

#exec --no-startup-id /usr/bin/dropbox
#exec --no-startup-id /usr/bin/screen -dmR
#exec --no-startup-id /usr/bin/gnome-volume-control-applet
#exec --no-startup-id /usr/bin/system-config-printer-applet
#exec --no-startup-id /usr/bin/xscreensaver



## autostart file support
##exec ~/.i3/startup
#
### Background wallpaper
#exec nitrogen --restore
#
### Keyboard indicator/switcher for tray
#exec fbxkb


#exec pgrep stalonetray > /dev/null || stalonetray -bg '#303030' -f 0 --max-geometry 5x1 --geometry 5x1-0+0 --grow-gravity W --icon-gravity E -i 16 --kludges force_icons_size --no-shrink true --scrollbars horizontal --skip-taskbar true --sticky true --window-layer bottom

