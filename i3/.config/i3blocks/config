# vim: filetype=config

align=center
color=#ffffff
separator=true
separator_block_width=15
command=/usr/local/libexec/i3blocks/$BLOCK_NAME

[capslock]
interval=once
signal=11

[lsyncd]
interval=60

#[xautolock]
#signal=13

[fedoraupdate]
interval=240

[deja-dup]
interval=120

[bandwidth -i ↓ -o ↑]
interval=5

separator_block_width=10
separator=false

[networks]
interval=5

[cpu_usage]
interval=10

separator_block_width=10
separator=false

[load_average]
interval=10

[memory]
interval=30

[memory]
instance=swap
interval=30

[disk]
instance=/
interval=30

[disk]
instance=/home
interval=30

[temperature]
interval=2

#[fanspeed]
#command=grep ^speed: /proc/acpi/ibm/fan | awk '{ print $2 }'
#interval=2

[battery]
interval=30

[volume]
interval=once
signal=10

separator_block_width=10
separator=false

[volume]
instance=Capture
interval=once
signal=10

[stopwatch_countdown]
interval=1
#instance=notify-send "Countdown is over!"

[time]
command=date +"%a, %b %d (%j) %kh%M" | sed 's/ 0/ /g;s/(0/(/g'
interval=30
